import { PInvertColourPipe } from './p-invert-color.pipe';

describe('PInvertColourPipe', () => {
  it('create an instance', () => {
    const pipe = new PInvertColourPipe();
    expect(pipe).toBeTruthy();
  });
});
