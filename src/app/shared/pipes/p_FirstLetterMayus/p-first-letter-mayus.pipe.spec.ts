import { PFirstLetterMayusPipe } from './p-first-letter-mayus.pipe';

describe('PFirstLetterMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new PFirstLetterMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
