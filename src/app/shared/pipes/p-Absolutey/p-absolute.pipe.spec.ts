import { PAbsolutePipe } from './p-absolute.pipe';

describe('PAbsolutePipe', () => {
  it('create an instance', () => {
    const pipe = new PAbsolutePipe();
    expect(pipe).toBeTruthy();
  });
});
