import { PFirstLetterLowerPipe } from './p-first-letter-lower.pipe';

describe('PFirstLetterLowerPipe', () => {
  it('create an instance', () => {
    const pipe = new PFirstLetterLowerPipe();
    expect(pipe).toBeTruthy();
  });
});
